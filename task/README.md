# task

Bun CLI for `Task` ontology instances.

## Commands

- `task tree [--crit]`
- `task next [-l, --limit <n>]`
- `task view <id-or-prefix>`
- `task take <id-or-prefix> <worker>`

## Install in PATH

```bash
cd /workspace/gdedit/tmp/task
bun install
bun link
```

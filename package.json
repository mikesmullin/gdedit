{
  "name": "gdedit",
  "version": "1.0.0",
  "description": "Game Data Editor - Browser-based ontology editor",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "bun run --watch src/server.js",
    "start": "bun run src/server.js",
    "build": "bun build src/server.js --outdir=dist"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
